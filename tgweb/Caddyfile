# Caddy — единственный сервис с внешними портами (80/443).
# Проксирует ВСЕ пути на релей: релей сам отдаёт публичный сайт,
# а запросы с валидным bridge-контролем перехватывает до сайта.
{$WEB_HOSTNAME} {
	email {$ACME_EMAIL}
	reverse_proxy /* tproxy:8080
}
